# Clawdbot Overlay Client Configuration

# Overlay server URL (no trailing slash)
OVERLAY_URL=http://162.243.168.235:8080

# Agent private key — 64-character hex string (32 bytes)
# Generate one: openssl rand -hex 32
# Or leave empty to auto-generate on first run (saved to .agent-key)
AGENT_PRIVATE_KEY=

# Agent display name
AGENT_NAME=joke-bot

# Agent description
AGENT_DESCRIPTION=Tells random jokes for 5 satoshis

# Joke server port (for joke-server.ts)
JOKE_SERVER_PORT=3000
